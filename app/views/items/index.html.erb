<h1>購入商品</h1>
<% if user_signed_in? %>
  <% @items.each do |item| %>
    <p><%= item.name %>:<%= item.price %>:<%= item.user.nick_name.present? ? "購入者 #{item.user.nick_name}":"会員No.#{item.user.id}" %></p>
  <% end %>
<% else %>
  <p>ログインしてください</p>
<% end %>
